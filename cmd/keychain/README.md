# [cloudeng.io/macos/cmd/keychain](https://pkg.go.dev/cloudeng.io/macos/cmd/keychain?tab=doc)


Usage of `keychain`

    provide access to local `keychain`s across multiple operating systems

     read - read items from the `keychain`
    write - write items to the `keychain`

